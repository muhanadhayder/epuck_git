#include <p30F6014A.h>#include <stdlib.h>//for random numbers#include "stdio.h"#include "string.h"#include "math.h"#include "e_epuck_ports.h"#include "e_init_port.h"#include "e_motors.h"#include "utility.h"#include "runfollowball.h"//#include "e_led.h"//#include "e_prox.h"//#include "e_ad_conv.h"#include "e_uart_char.h"//#include "e_randb.h"//#include "btcom.h"//#include "e_remote_control.h"//#include "e_agenda.h"int sel;//the position of the selector switchint main(void){	myWait(500);//saftey wait period to prevent UART clogging	e_init_port();	sel = getselector();		//init uart for randb comm	e_init_uart1();	//e_init_prox();if(sel == 0){	    while(1){			setSpeeds(700, 0);		        myWait(5);	    }}if(sel == 1){	    while(1){			setSpeeds(700, 700*0.1);		        myWait(5);	    }}if(sel == 2){	    while(1){			setSpeeds(700, 700*0.2);		        myWait(5);	    }}if(sel == 3){	    while(1){			setSpeeds(700, 700*0.3);		        myWait(5);	    }}if(sel == 4){	    while(1){			setSpeeds(700, 700*0.4);		        myWait(5);	    }}if(sel == 5){	    while(1){			setSpeeds(700, 700*0.5);		        myWait(5);	    }}if(sel == 6){	    while(1){			setSpeeds(700, 700*0.6);		        myWait(5);	    }}if(sel == 7){	    while(1){			setSpeeds(700, 700*0.7);		        myWait(5);	    }}if(sel == 8){	    while(1){			setSpeeds(700, 700*0.8);		        myWait(5);	    }}if(sel == 9){	    while(1){			setSpeeds(700, 700*0.9);		        myWait(5);	    }}if(sel == 10){	    while(1){			setSpeeds(700, 700);		        myWait(5);	    }}if(sel == 11){	    while(1){			setSpeeds(700, -700);		        myWait(5);	    }}if(sel == 12) while(1) NOP(); //in case it gets here, it shouldn't fall off the edgewhile(1) NOP();}